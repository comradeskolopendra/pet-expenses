import { FC } from "react";

import ExpensesForm from "./components/expenses-form/expenses-form";

const Main: FC = () => {
    return (
        <section>
            <ExpensesForm />
        </section>
    )
};

export default Main;